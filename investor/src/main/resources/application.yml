spring:
  application:
   name: investor

  datasource:
    url: jdbc:postgresql://localhost:5432/fundbridge
    username: ajay
    password: ajay
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect